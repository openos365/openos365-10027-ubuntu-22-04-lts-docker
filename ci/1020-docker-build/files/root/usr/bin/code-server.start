#!/usr/bin/env bash

set -x
export CMD_PATH=$(cd `dirname $0`; pwd)
export PROJECT_NAME="${CMD_PATH##*/}"
echo $PROJECT_NAME
cd $CMD_PATH
export PASSWORD=openos365
export PORT=18080

code-server --password $PASSWORD --bind-addr 0.0.0.0:$PORT ~/git/
